# 2026-06-06 工作日志

## time_tracker 项目

### GitHub Actions CI 修复
- **问题**: build-electron.yml 和 build-mobile.yml 中 `actions/setup-node@v4` 配置了 `cache: 'pnpm'`，但仓库缺少 `pnpm-lock.yaml`，导致 Setup Node.js 步骤失败
- **修复**: 移除两处 `cache: 'pnpm'` 配置，提交 `0212e27`
- **状态**: 修复已推送，新 run #2 正在执行（截至 16:27 UTC+8 Mobile 构建进行中，Electron 排队中）

### 中文界面翻译
- 从远程拉取最新代码（79e2554），解决 LoginPage/RegisterPage 冲突
- 翻译剩余英文 UI 文本共计 14 处，涉及 6 个文件：
  - ActivityCard.tsx: Edit→编辑, Delete→删除
  - ActivityList.tsx: Activities→活动, New Activity→新建活动
  - ActivityForm.tsx: 并行模式标签翻译
  - StatsPanel.tsx: Statistics→统计, Day/Week/Month→今日/本周/本月, Total Duration→总时长, Activities→活动数, Time Distribution→时间分布
  - Timeline.tsx: Timeline→时间线
  - SettingsPage.tsx: N/A→无 (2处)
- 提交 b952caa 已推送 GitHub

### WebSocket 连接修复
- **问题**: 部署到 192.168.40.132 后登录显示"连接中"而非"已连接"
- **根因**: `useWebSocket.ts` 中 WebSocket URL fallback 硬编码为 `ws://localhost:8000/api/ws`，部署后浏览器尝试连本地 localhost 而非服务器
- **修复**: 改为从 `window.location` 自动推导（`ws://${host}/api/ws`），零配置适配任何部署环境
- 提交 9b821e7 已推送 GitHub
